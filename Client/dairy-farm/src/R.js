import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

const R = createSlice({
  name: second,
  initialState,
  reducers: {
    
  }
});

export const {} = R.actions

export default R.reducer